# Core web app dashboard framework
streamlit==1.35.0

# NLP and linguistic processing
spacy==3.7.4
nltk==3.8.1

# Classical topic modeling
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2

# Interactive charts and graphs
plotly==5.22.0
pyvis==0.3.2

# PDF compilation engine
reportlab==4.2.0
Pillow==10.3.0

# Note: Standard libraries below are built-in and do not need installation:
# pathlib (stdlib)
# json (stdlib)
# re (stdlib)
# collections (stdlib)

# After pip install, run these once to download required models:
# python -m spacy download en_core_web_sm
# python -c "import nltk; nltk.download('vader_lexicon')"
